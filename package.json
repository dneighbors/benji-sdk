{
  "name": "benji-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "generate": "pnpm --filter @dneighbors/benji-sdk generate",
    "release": "pnpm build && pnpm -r publish --access public"
  },
  "engines": {
    "node": ">=20.19.0",
    "pnpm": ">=9"
  }
}
